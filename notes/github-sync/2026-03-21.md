# GitHub Sync — foodVibe 1.0 — 2026-03-21

## Summary

- **Commits**: ~19 (last 7 days, local `git log`) | **PRs Merged**: (not listed — `gh pr list` returned no rows in this environment) | **PRs Open**: — | **Issues Open**: —
- **Branch**: `main` — **ahead of `origin/main` by 13 commits** (local commits not pushed)
- **Working tree**: 48 paths marked modified + 1 untracked plan file; `git diff` / `git diff HEAD` show **substantive content changes only in** `.claude/settings.local.json` — other modified paths match HEAD for content (typical CRLF / index refresh noise on Windows; refresh with `git status` after `git add` / checkout if needed)

## Key Changes

### Recent local commits (high level)

Merge and feature work including: duplicate-name validation for venues/suppliers, inventory filter spec stabilization, shared selection/empty-state refactors, dashboard test IDs, template/i18n token work, agent workflow and plan/todo updates.

### Hot files (from current session scope)

- Registry and app shell: `unit-registry`, `preparation-registry`, `metadata-registry`, `user.service`, `app.config.ts`
- Lists/forms: inventory product list & product form, equipment, suppliers, venues, recipe book, recipe builder workflow & header, metadata manager, cook view, dashboard
- Shared: `unit-creator`, global `styles.scss`
- Data: `public/assets/data/dictionary.json`
- Local: `.claude/settings.local.json`

### Open PRs / Open Issues

| # | Title | Notes |
|---|--------|--------|
| — | — | Run `gh pr list` / `gh issue list` locally if GitHub CLI is authenticated |

## Impact on Current Work

- **Ahead of origin**: Push when ready so remote matches local `main`.
- **Large “modified” list with tiny diff**: Before committing, confirm whether you want to normalize line endings or `git restore` paths that should not change.
