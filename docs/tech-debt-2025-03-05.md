# Tech Debt Report — foodVibe — 2025-03-05

## Critical (Fix Now)

- [ ] **recipe-builder.page.ts (964 lines)**: File far exceeds 300-line standard — hurts maintainability — Split into feature modules / child components or extract services.
- [ ] **menu-intelligence.page.scss (824)**, **recipe-book-list.component.scss (713)**, **cook-view.page.scss (710)**, **product-form (scss 704, ts 734)**: Multiple files 2–3× over limit — Refactor into smaller SCSS/component units.

## High Priority (Fix This Sprint)

- [ ] **recipe-ingredients-table.component.ts:42**: `@Input() focusSearchAtRow` — Migrate to `input()` (Angular modern API).
- [ ] **recipe-workflow.component.ts:42**: `@Input() focusRowAt` — Migrate to `input()`.
- [ ] **supplier-form.component.ts:35–37**: `@Input() embeddedInDashboard`, `@Output() saved`, `@Output() cancel` — Migrate to `input()` / `output()`.
- [ ] **venue-form.component.ts:47–49**: Same pattern — Migrate to `input()` / `output()`.
- [ ] **cell-carousel.component.ts:43,47**: `@Input() set activeIndex`, `@Output() activeIndexChange` — Migrate to `input()` / `output()`.
- [ ] **click-out-side.ts:10**: `@Output() clickOutside` — Migrate to `output()`.
- [ ] **app.component.spec.ts:21–23**: Stub component uses `@Input`/`@Output` — Acceptable in spec; consider aligning with real component style when touching.
- [ ] **conversion.service.ts:42,54**: `wastePercent: any`, `yieldFactor: any` — Replace with `number` (or union) for type safety.
- [ ] **ingredient-search.component.ts:70**: `selectItem(item: any)` — Use a proper interface/type.
- [ ] **recipe-ingredients-table.component.ts:93,234**: `onItemSelected(item: any)`, `(item as any)?.yield_percentage` — Type ingredient/item model.
- [ ] **recipe-header.component.ts:181**: `(c: any)` in callback — Type conversion/unit model.
- [ ] **menu-intelligence.page.ts:678**: `(section: any, ...)` — Type section model.
- [ ] **recipe-builder.page.ts:578**: `createIngredientGroup(item: any = null)` — Type parameter.

## Medium Priority (Track)

- [ ] **ingredient-search.component.html:11,28**: Hardcoded Hebrew placeholder and labels ("חפש מוצר או מתכון", "מתכון", "מוצר") — Move to `translatePipe` / dictionary.
- [ ] **metadata-manager.page.component.html**: Multiple hardcoded Hebrew strings (headings, buttons, empty states) — Move to translation keys.
- [ ] **unit-creator.component.html**: Hardcoded Hebrew labels and buttons — Move to translation keys.
- [ ] **ingredient-search.component.scss:6**, **recipe-ingredients-table.component.scss:161**, **recipe-header.component.scss:148**: TODOs re `::ng-deep` — Remove when scrollable-dropdown/custom-select expose slots or engine class.
- [ ] **app.component.ts:5**, **product-form.component.ts:13**, **app.component.spec.ts:7**, **filter-category.model.ts:1**: Lines with `";` (semicolon after string) — Project prefers no semicolons; clean up if style guide applies to imports.

## Low Priority (Nice to Have)

- [ ] **Spec files**: `(component as any)` and `as any` in tests — Prefer typed access (e.g. public test hooks or cast to minimal interface) where it doesn’t hurt test clarity.
- [ ] **kitchen-state.service.spec.ts:100**, **pending-changes.guard.spec.ts**: `} as any` for mocks — Keep for brevity or replace with minimal typed mocks.

## Metrics

| Metric | Count |
|--------|--------|
| Total TODOs | 3 |
| Duplicate code blocks | Not quantified (manual/explore follow-up) |
| Files over 300 lines | 20 |
| @Input/@Output (legacy) | 7 components (multiple I/O each) |
| BehaviorSubject | 0 |
| Inline `style=` in HTML | 0 |
| `any` types (non-spec) | ~8 files (conversion, ingredient-search, recipe-ingredients-table, recipe-header, menu-intelligence, recipe-builder) |
| Semicolons `";` in TS | 4 files |
| Hardcoded Hebrew strings | 25+ lines across ingredient-search, metadata-manager, unit-creator |

## Suggested copilot-instructions addition

- **Pattern**: Legacy `@Input()` / `@Output()` still present in multiple components.
- **Rule**: When touching a component that uses `@Input()` or `@Output()`, migrate those members to `input()` and `output()` from `@angular/core` and update template bindings to the new API.

## Related Skills

- `/update-docs` — Update documentation after cleanup
- `/elegant-fix` — Refine solutions found during audit
- `/github-sync` — Review recent changes for debt introduction
