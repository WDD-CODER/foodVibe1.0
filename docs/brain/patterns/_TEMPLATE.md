# Pattern: {the rule, not the API name}

> Template — copy into a new `patterns/{kebab-name}.md` and delete this line. One screen max; syntax and checklists stay in `docs/agent/*.md` — link them instead of inlining. Drafting rules: `docs/agent/brain-capture.md`.

## Problem

{What kept going wrong or costing time without this pattern — the situation and the cost, not the API.}

## Solution

{The repeatable recipe. Numbered steps if order matters. Include the audit/exception rules — what to check before applying, and what deliberately stays out of scope.}

## When to use

{The trigger situation — and when NOT to use it. Prefer extending this pattern over inventing a parallel one.}

See also: {paired gotcha / related ADR as [[wiki-links]], if any}
