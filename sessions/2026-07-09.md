# Session — 2026-07-09

## Milestone: Workflow System Reconciliation — Phase 4 residual (overdue decision closeout)

**Branch:** `chore/three-agent-cutover`  
**Scope:** docs/workflow only — no `src/`, no `server/`  
**Status:** READY FOR REVIEW — STOP

### Files touched
- `.claude/todo.md` — Workflow Evolution section: resolved 4 overdue checkboxes with dated outcomes
- `sessions/2026-07-09.md` — this handoff

### Decisions
1. **Reflect test-drive (2026-04-28) + Reflect system verdict (2026-05-04):** **PARK / superseded by three-agent cutover**. Confirmed `.claude/reflect/` and `.claude/reflect/test-drive/` absent. Did not recreate or delete reflect files. Default PARK for under-logged runs applied retrospectively.
2. **end-session reflect reminder (2026-04-28):** marked **obsolete / superseded** (skill already removed in cutover).
3. **Agent roster review (2026-05-04 → closed 2026-07-09):** keep all 6 agents; no consolidation/removals this pass.
   - Kept: `git-agent`, `mobile-flow-auditor`, `qa-engineer`, `render-flow-auditor`, `security-officer`, `team-leader`
   - Cross-check: each referenced by live `.claude/commands/` and/or `CLAUDE.md` / `.cursorrules`
   - `invocation-log.tsv` only has pre-cutover `scss-implementer` rows — not used to justify further cuts

### Lint / tests
N/A (docs-only milestone)

### Reviewer scrutiny
- Confirm Workflow Evolution items are closed with outcomes (not still pending deadlines)
- Confirm no app code (`src/`, `server/`) was touched
- Do **not** start Phase 5 or Phase 6 from this handoff

### Explicitly not done
- Phase 5, Phase 6
- Commit / push (Human's job)
- Any reflect file recreation or Plan 247 reflect checklist edits

---

## Milestone: Workflow System Reconciliation — Phase 6 residual (mobile audit status report)

**Branch:** `chore/three-agent-cutover`  
**Scope:** verification/report only — no `src/`, no `server/`, no component SCSS  
**Status:** READY FOR REVIEW — STOP

### Files touched
- `.claude/reports/mobile-audit/2026-07-status.md` — **created** (plans 276–283 done vs open + git/code evidence)
- `.claude/reports/mobile-audit/TRIAGE.md` — Phase 6 status stamp only (no defect-count or cluster-resolved flips)
- `sessions/2026-07-09.md` — this handoff append

### Not touched (intentionally)
- `.claude/reports/mobile-audit/INDEX.md` — no new run data
- `.claude/reports/mobile-audit/cook-view/report.md` — stub left as-is (malformed; needs live re-audit)
- Any plan file checkboxes / todo verify boxes — Contractor does not mark milestones complete

### cook-view re-audit
**Did not run.** Preflight failed: `http://localhost:4200/` unreachable; API/Mongo unreachable. Per contract: do not fabricate; document manual follow-up.

### Decisions / findings
1. All eight plan files (`plans/276`–`283`) exist; Atomic Sub-task boxes in plan files are still unchecked.
2. `.claude/todo.md` shows **impl `[x]` / verify `[ ]`** for all eight.
3. Primary git: `a9c56f7` (276–283 bundle); `d381686` (Plan 280); `09c9ff2` overstated “complete”; Plan 278 financial-bar later removed in `ba9cf5b`.
4. Code spot-check confirms FAB, grid actions, sticky padding, touch targets, RTL trash margin, input ellipsis, chip-search close/40vh/Escape, duplicate-name ID exclusion still present.

### Lint / tests
N/A (docs/report-only; no app changes)

### Reviewer scrutiny
- Confirm no `src/` / `server/` edits
- Confirm cook-view stub was **not** inventively filled
- Confirm INDEX counts unchanged
- Confirm status table honesty: impl vs verify split

### Explicitly not done
- Phase 4 / Phase 5 (out of scope for this handoff)
- Live `/mobile-flow-audit --only cook-view`
- Commit / push (Human's job)

---

## Milestone: Workflow System Reconciliation — Phase 5 residual (JWT_SECRET fallback removal)

**Branch:** `chore/jwt-secret-fallback-removal` (created from `chore/three-agent-cutover` HEAD)  
**Scope:** server auth secrets only — no `src/app`  
**Status:** READY FOR REVIEW — STOP (local full auth E2E blocked; code + static verify done)

### Files touched
- `server/middleware/auth.js` — `ACCESS_SECRET = process.env.JWT_ACCESS_SECRET` only; comment updated to JWT_ACCESS_SECRET
- `server/routes/auth.js` — `ACCESS_SECRET` / `REFRESH_SECRET` use dedicated env vars only (no `|| process.env.JWT_SECRET`)
- `server/db.js` — boot gate now requires `JWT_ACCESS_SECRET` and `JWT_REFRESH_SECRET` (replaces legacy `JWT_SECRET` check)
- `sessions/2026-07-09.md` — this Phase 5 append

### Not touched (intentionally)
- `src/` / client — none
- Real `.env` — never read/printed; file ABSENT in this workspace
- `.env.example` — ABSENT in repo; nothing to update
- `render.yaml` — still documents `JWT_SECRET` only; **left unchanged** (Human follow-up after local pass; do not remove Render `JWT_SECRET` yet)
- Commit / push / Render deploy — not done

### Grep confirmation
- `|| process.env.JWT_SECRET` fallback in `server/`: **NONE**
- Any `JWT_SECRET` string in `server/**/*.js`: **NONE**
- `node --check` on the three scoped files: **SYNTAX_OK**

### Decisions
1. Removed dual-secret fallbacks; dedicated secrets only.
2. `db.js` was a legacy boot gate on `JWT_SECRET` — updated to require both dedicated secrets so missing config fails at startup instead of silent fallback.
3. Did not invent new secret architecture beyond matching existing dedicated-secret names already used in auth code.

### Local regression checklist
| Check | Result |
| --- | --- |
| Server starts with dedicated secrets present | **NOT RUN** — `server/node_modules` ABSENT; root `.env` ABSENT |
| New user signup issues valid token | **NEEDS HUMAN** |
| Existing user login succeeds | **NEEDS HUMAN** |
| Logout clears session | **NEEDS HUMAN** |
| Token refresh on expiring token | **NEEDS HUMAN** |
| Guest login if autoLoginGuest enabled | **NEEDS HUMAN** |
| Protected route 401 unauthenticated | **NEEDS HUMAN** |
| Protected route 401 with token signed under OLD fallback secret | **NEEDS HUMAN** (confirms fallback gone) |
| `ng build` | **N/A** — no client changes |
| Server syntax | **PASS** (`node --check` on scoped files) |

### Human follow-up (before Render)
1. Ensure local `.env` has `JWT_ACCESS_SECRET` and `JWT_REFRESH_SECRET` (do not rely on `JWT_SECRET`).
2. `npm install --prefix server` if needed; run `npm run dev:local` in server and walk the auth checklist above.
3. After local pass: add `JWT_ACCESS_SECRET` + `JWT_REFRESH_SECRET` on Render; keep existing `JWT_SECRET` until cutover verified — **do not remove Render `JWT_SECRET` yet**; update `render.yaml` in a later Human/Architect step.
4. Commit / push on this branch when ready (Human).

### Reviewer scrutiny
- Confirm no `|| process.env.JWT_SECRET` left under `server/`
- Confirm `db.js` boot messages name the dedicated secrets
- Confirm no client / Render / `.env` edits
- Confirm branch is `chore/jwt-secret-fallback-removal`, not `chore/three-agent-cutover`

### Explicitly not done
- Commit / push
- Render env / `render.yaml` changes
- Full auth E2E (blocked: no local `.env`, no `server/node_modules`)
- Phase 6 (separate; already handed off elsewhere on cutover branch)

---

## /ship wrap — 2026-07-09 (chore/three-agent-cutover)

**Branch:** `chore/three-agent-cutover`
**Date:** 2026-07-09
**Summary:** Close overdue Workflow Evolution decisions (Phase 4) and add mobile-audit status report (Phase 6). Docs/workflow only.
**Files to be committed:**
- .claude/todo.md
- .claude/reports/mobile-audit/2026-07-status.md
- .claude/reports/mobile-audit/TRIAGE.md
- .claude/reports/mobile-audit/cook-view/report.md
- sessions/2026-07-09.md
**Verify:**
- Open .claude/todo.md Workflow Evolution — four items show RESOLVED 2026-07-09 with PARK/superseded or keep-roster outcomes
- Open .claude/reports/mobile-audit/2026-07-status.md — status report present; verify still open noted
- git diff --stat HEAD after ship — no src/ or server/ in this commit
**Commit:** included in this /ship commit
**Push:** yes

---

## Milestone: Brief C — Skill Discovery UX (`/skills`)

**Branch:** `chore/skills-discovery-command`  
**Scope:** workflow docs/commands only — no `src/`, no `server/`  
**Status:** READY FOR REVIEW — STOP

### Files touched
- `.claude/commands/skills.md` — new `/skills` command (live skill registry table)
- `.claude/commands/_index.md` — added `/skills` row under Maintenance & Cleanup
- `CLAUDE.md` — Skill discovery trigger (replaces retired `copilot-instructions.md` §0)
- `sessions/2026-07-09.md` — this handoff

### Decisions
1. **Adapted Brief C for post-cutover:** `copilot-instructions.md` is gone. Trigger lives in `CLAUDE.md` instead of §0.
2. **Registry source of truth:** on-disk `.claude/skills/*/SKILL.md` only (17 skills). Skipped `cssLayer-workspace` (no `SKILL.md`).
3. **Did not list retired skills** (`mp-search`, `nightly-audit`, `worktree-session-end`, `execute-debugging`, etc.).
4. **Scope tags:** SHARED for Contractor+Reviewer skills; `brief-detection` tagged CC (slash/command gate path).
5. **Branch isolation:** work done on `chore/skills-discovery-command` (not JWT branch name in handoff title). Tip still includes prior JWT commit history from checkout base — Reviewer should scope to the four files above only.

### Lint / tests
N/A (markdown-only). `ng build` not required for command docs.

### Reviewer scrutiny
- Confirm table matches live skill dirs (no ghosts, no duplicates)
- Confirm `_index.md` + `CLAUDE.md` wiring only — no MemPalace / team-leader Brief A/B
- Confirm no app code touched

### Explicitly not done
- Brief A (Team Leader ghost-reporting) — obsolete post-cutover
- Brief B (MemPalace diary) — obsolete post-cutover
- Commit / push (Human's job)

---

## Milestone: Command Discovery UX (`/commands`)

**Branch:** `chore/skills-discovery-command`  
**Scope:** workflow docs/commands only — no `src/`, no `server/`  
**Status:** READY FOR REVIEW — STOP

### Files touched
- `.claude/commands/commands.md` — new `/commands` registry (mirror of `/skills`)
- `.claude/commands/_index.md` — `/commands` row
- `CLAUDE.md` — command discovery trigger beside skills
- `sessions/2026-07-09.md` — this handoff

### Decisions
1. Same pattern as `/skills`: on-disk `.claude/commands/*.md` only; categories FLOW vs UTIL.
2. Includes self (`/commands`) and `/skills` in the table.
3. Retired cutover commands listed in Notes only (not in live table).

### Explicitly not done
- (superseded by /ship below)

---

## /ship wrap — 2026-07-09 (chore/skills-discovery-command)

**Branch:** `chore/skills-discovery-command`
**Date:** 2026-07-09
**Summary:** Add `/skills` and `/commands` discovery commands; wire into `_index.md` and `CLAUDE.md` (post-cutover replace for retired copilot-instructions §0).
**Files to be committed:**
- `.claude/commands/skills.md`
- `.claude/commands/commands.md`
- `.claude/commands/_index.md`
- `CLAUDE.md`
- `sessions/2026-07-09.md`
**Verify:**
- Open `.claude/commands/skills.md` — 17 live skills, no retired names
- Open `.claude/commands/commands.md` — includes `/skills` and `/commands`
- Open `_index.md` — both `/skills` and `/commands` rows under Maintenance
- Open `CLAUDE.md` — Skill & command discovery section present
- `Test-Path` both new command files — True
**Commit:** included in this /ship commit
**Push:** yes
**Note (superseded):** JWT history pollution fixed in Follow-ups below.

---

## Follow-ups — 2026-07-09 (post `/evaluate-me`)

**Branch:** `chore/skills-discovery-command` (rebuilt from `origin/chore/three-agent-cutover`)  
**Status:** READY FOR REVIEW — STOP

### Done
1. **History hygiene:** reset to cutover tip + cherry-pick discovery only — JWT commit no longer on this branch (remains on `chore/jwt-secret-fallback-removal`).
2. **Session-state path:** created `docs/session-state-chore-skills-discovery-command-1.md`; kept `.claude/.session-state-path` pointer update.
3. **`/ship`:** added §2.2.1 unrelated-commits-on-branch warning.
4. **`CLAUDE.md`:** note that discovery replaces retired `copilot-instructions.md` §0.
5. **`/skills` + `/commands`:** mandatory disk count check before printing stale tables.
6. Retrospective saved: `.claude/retrospectives/2026-07-09-13-32-contractor.md`

### Files in follow-up commit
- `CLAUDE.md`
- `.claude/commands/ship.md`
- `.claude/commands/skills.md`
- `.claude/commands/commands.md`
- `.claude/.session-state-path`
- `docs/session-state-chore-skills-discovery-command-1.md`
- `.claude/retrospectives/2026-07-09-13-32-contractor.md`
- `sessions/2026-07-09.md`

### Explicitly not done
- Merge to `main` (Human)
- JWT PR (separate branch — already on origin)

---

## Milestone: `/commands` + `/skills` fast-path registry

**Branch:** (current feature/chore branch — do not write on `main`)  
**Scope:** `.claude/commands/commands.md`, `.claude/commands/skills.md` only  
**Status:** READY FOR REVIEW — STOP

### Files touched
- `.claude/commands/commands.md` — default print embedded table; optional one Glob filename-set check; `--refresh` full rebuild; no MCP
- `.claude/commands/skills.md` — same pattern; retired skill dirs excluded from mismatch set; `--refresh` full rebuild
- `sessions/2026-07-09.md` — this handoff

### Decisions
1. Replaced “read every file / mandatory count rebuild” with **print embedded registry** + **cheap filename-set Glob**.
2. Count-only checks are insufficient for skills if retired folders reappear — use **set compare** + explicit retired exclude list.
3. Description text drift is accepted until `--refresh` or a manual table edit when adding/removing entries.

### Lint / tests
N/A (docs/workflow only)

### Reviewer scrutiny
- Confirm default path forbids per-file reads and MCP
- Confirm mismatch / `--refresh` still rebuilds correctly
- Confirm retired skills stay out of the printed table

### Explicitly not done
- Updating `_index.md` (unchanged; still human-oriented twin)

---

## /ship wrap — 2026-07-09 (fast-path registries)

**Branch:** `chore/skills-discovery-command`
**Date:** 2026-07-09
**Summary:** Fast-path `/skills` and `/commands` — print embedded registry by default; cheap Glob set-check; full rebuild only on mismatch or `--refresh`.
**Files to be committed:**
- `.claude/commands/skills.md`
- `.claude/commands/commands.md`
- `sessions/2026-07-09.md`
**Verify:**
- Open skills.md — default path says print table; no per-file SKILL reads / no MCP
- Open commands.md — same fast path + Refresh path on mismatch/--refresh
- Confirm retired skill dirs listed as excluded from mismatch set
- Diff sessions/2026-07-09.md — fast-path milestone handoff present
- git log origin/chore/three-agent-cutover..HEAD — discovery-only (no JWT)
**Commit:** included in this /ship commit
**Push:** no

## /ship wrap — 2026-07-09 (chore/jwt-secret-fallback-removal)

**Branch:** `chore/jwt-secret-fallback-removal`
**Date:** 2026-07-09
**Summary:** Remove legacy JWT_SECRET fallback; require JWT_ACCESS_SECRET and JWT_REFRESH_SECRET only (Phase 5).
**Files to be committed:**
- server/db.js
- server/middleware/auth.js
- server/routes/auth.js
- sessions/2026-07-09.md
**Verify:**
- Grep server/ for JWT_SECRET — expect none
- Open auth middleware/routes — ACCESS/REFRESH secrets from dedicated env only
- Open db.js boot gate — requires JWT_ACCESS_SECRET and JWT_REFRESH_SECRET
- node --check on the three server files — SYNTAX_OK
**Commit:** included in this /ship commit
**Push:** yes

## /ship wrap — 2026-07-09 (chore/pr140-survivors)

**Branch:** `chore/pr140-survivors`
**Date:** 2026-07-09
**Summary:** Land PR #140 survivors without GitHub Actions keepalive. Add in-app SPA heartbeat (Page Visibility + /api/v1/health every 4 min while visible). AI recipe quality-warning gate, dictionary keys, orphan gemini-shots.util removal, mongoose pool options.
**Files to be committed:**
- src/app/core/services/server-heartbeat.service.ts
- src/app/appRoot/app.component.ts
- src/app/shared/ai-recipe-modal/ai-recipe-modal.component.ts
- src/app/shared/ai-recipe-modal/ai-recipe-modal.component.html
- public/assets/data/dictionary.json
- src/app/core/utils/gemini-shots.util.ts (deleted)
- server/db.js
- sessions/2026-07-09.md
**Verify:**
- Open SPA on Render — Network shows GET /api/v1/health ~every 4 min while tab visible
- Hide tab 2+ min — health pings stop (Free tier can sleep)
- Approve AI draft with soft warnings — see quality warnings + continue/back
- Grep for gemini-shots.util — zero imports; file gone
- Confirm no .github/workflows/render-keepalive.yml in tree
- Open server/db.js — JWT_ACCESS/REFRESH gates intact; minPoolSize/maxPoolSize present
**Commit:** included in this /ship commit
**Push:** yes


---

## /ship wrap — 2026-07-09 (feat/session-20260709)

**Branch:** `feat/session-20260709`
**Date:** 2026-07-09
**Summary:** Point session-state capsule at feat/session-20260709; create capsule file. Local server/.env remains gitignored (not committed).
**Files to be committed:**
- `.claude/.session-state-path`
- `docs/session-state-feat-session-20260709-1.md`
- `sessions/2026-07-09.md`
**Verify:**
- Open `.claude/.session-state-path` — points to docs/session-state-feat-session-20260709-1.md
- Capsule file exists
- `git check-ignore server/.env` — ignored (not in commit)
**Commit:** included in this /ship commit
**Push:** yes

---

## Milestone 1 — Save Latency Fix (Version History Bottleneck)

**Plan:** Save Latency Fix — scoped version queries + targeted delete
**Milestone:** 1 — Server: scoped version queries + targeted delete
**Status:** Ready for review (not marked complete in plan — Human/Reviewer marks)
**Branch:** `feat/session-20260709`

### Files touched
- `server/routes/generic.js` — optional `?filterEntityType=&filterEntityId=` on GET `/:type`; new `DELETE /:type/bulk` (`{ ids: string[] }`, `verifyToken`, `deleteMany` scoped to `userId`). Registered **before** `DELETE /:type/:id` so `bulk` is not treated as an id.
- `server/db.js` — idempotent compound index on `VERSION_HISTORY`: `{ userId: 1, entityType: 1, entityId: 1 }` (same `createIndex` pattern as existing CLONEABLE_TYPES indexes; VERSION_HISTORY is not in CLONEABLE_TYPES).

### Decisions
- Filter fields match `VersionEntry` shape: `entityType`, `entityId` (confirmed in `version-history.service.ts`).
- Absent filter params → GET behavior unchanged (same `userId` + `_userDeleted` filter).
- PUT `/:type` (replaceAll) left untouched per plan.
- No other route file already handled VERSION_HISTORY filters (`server/breadcrumbs.md` confirms generic.js is the CRUD surface).
- Bulk delete returns `{ ok: true, deletedCount }`; validates non-empty string `ids` array.

### Reviewer should scrutinize
- Route order: `DELETE /:type/bulk` before `DELETE /:type/:id`.
- Cross-user safety: bulk delete always includes `userId: req.user.userId`.
- Index creation only for `VERSION_HISTORY` — not added to `CLONEABLE_TYPES` (correct; that list is for clone/sync).

### Lint / verify
- `node --check` on `generic.js` and `db.js`: clean.
- Manual API verify (Reviewer / Human):
  - `GET /api/v1/data/VERSION_HISTORY?filterEntityType=recipe&filterEntityId=abc12` → only that entity's docs
  - `DELETE /api/v1/data/VERSION_HISTORY/bulk` with `{ "ids": [...] }` → only those ids for auth user
  - Restart server once so `db.js` creates the compound index (log: `VERSION_HISTORY compound index ensured`)

### STOP
Milestone 1 complete for Contractor handoff. Awaiting explicit instruction before Milestone 2.

---

## Milestone 2 — Save Latency Fix (Client scoped addVersion + non-blocking save)

**Plan:** Save Latency Fix — Version History Bottleneck
**Milestone:** 2 — Client: rewrite addVersion() to be scoped and non-blocking
**Status:** Ready for review (not marked complete — Human/Reviewer marks)
**Branch:** `feat/session-20260709`

### Files touched
- `src/app/core/services/http-storage.adapter.ts` — `queryFiltered()`, `deleteBulk()`
- `src/app/core/services/async-storage.service.ts` — pass-through `queryFiltered` / `deleteBulk` (backend + localStorage filter/remove helpers)
- `src/app/core/services/version-history.service.ts` — backend `addVersion`: `post` + `queryFiltered` + overflow `deleteBulk`; localStorage keeps full-array `replaceAll`
- `src/app/core/services/kitchen-state.service.ts` — `saveRecipe` / `saveProduct`: real save first, then fire-and-forget `addVersion` (failures logged via `LoggingService`, do not block toast)

### Decisions
- `saveProduct` had the same blocking `addVersion` → `switchMap` pattern; fixed in the same milestone (same bug class as `saveRecipe`).
- Fire-and-forget `.catch` logs all version-write failures including 401; `addVersion` itself still throws 401 for any awaiting caller. Save confirmation is never blocked.
- `MAX_VERSIONS_PER_ENTITY` (20) unchanged. `restoreVersion` / `getVersions` / `addVersionAsNewRecipe` untouched.

### Reviewer should scrutinize
- Backend `addVersion` posts then re-queries entity versions (includes the new doc) before trimming oldest overflow.
- `typeChanged` path: `this.deleteRecipe(previous!)` (Observable) — not wrapped in `from()`.
- Hebrew success toasts still hardcoded in kitchen-state (pre-existing; out of scope).

### Lint / verify
- `ng lint`: no lint target configured in this project (`Cannot find "lint" target`).
- `npx tsc -p tsconfig.app.json --noEmit`: clean.
- Manual: save recipe/dish/product → toast after entity write; Network shows scoped `VERSION_HISTORY?filterEntityType=…&filterEntityId=…` not full collection GET + replaceAll.

---

## Milestone 3 — Audit replaceAll callers (report only — no code changes)

**Status:** Findings only. No duplicate of the VERSION_HISTORY-on-every-save anti-pattern found. No speculative fixes.

### Findings

| Site | Verdict | Notes |
|------|---------|-------|
| `version-history.service.ts:104` | Fixed (M2) / localStorage OK | Backend path no longer uses `replaceAll`. localStorage full-array trim is intentional (plan: not perf-critical). |
| `recipe-data.service.ts:151,167,184,196` | Safe (bounded) | `TRASH_RECIPES` only — trash list rewrite after restore/dispose. |
| `dish-data.service.ts:158,174,191,203` | Safe (bounded) | `TRASH_DISHES` only. |
| `product-data.service.ts:193,209,226,238` | Safe (bounded) | `TRASH_PRODUCTS` only. |
| `equipment-data.service.ts:109` | Safe (bounded) | Trash restore rewrite. |
| `venue-data.service.ts:93` | Safe (bounded) | Trash restore rewrite. |
| `preparation-registry.service.ts:78` | Safe (bounded) | Always replaces with a **single-doc** array `[{ … }]`. |
| `demo-loader.service.ts:59–70` | Intentional full replace | Demo seed only — infrequent, not on save path. |
| `backup.service.ts:153` | Intentional full replace | Backup **import** only — infrequent, not on save path. |
| `trash.service.ts` | N/A | No `replaceAll` / query-rewrite pattern. |
| `kitchen-state` supplier/equipment/venue saves | N/A | No version-history / `replaceAll` on those save paths. |

### Conclusion
The save-path bottleneck was a one-off in `VersionHistoryService.addVersion` (+ its `saveRecipe`/`saveProduct` call sites). Trash/demo/backup `replaceAll` uses are bounded or intentional bulk ops — **no new milestone needed** unless product later wants to optimize trash restore similarly.

### STOP
All three milestones executed. Awaiting Human/Reviewer mark-complete.

---

## Milestone 4 — App Load Optimization (Defer root modals)

**Plan:** `plans/289-app-load-optimization.plan.md`
**Milestone:** 4 — Defer modal components out of the initial bundle
**Status:** Ready for review
**Branch:** `feat/session-20260709`

### Bundle sizes (`ng build --configuration production`)
| | Before | After | Delta |
|--|--------|-------|-------|
| Initial total | 920.70 kB | 670.83 kB | **-249.87 kB** |
| main | 261.57 kB | 120.69 kB | **-140.88 kB** |
| Transfer (est. initial) | 219.06 kB | 173.51 kB | **-45.55 kB** |

### Files touched
- `src/app/appRoot/app.component.html` — `@defer (when <openSignal>)` around 13 modals
- `src/app/appRoot/app.component.ts` — inject open-signal services for `@defer` conditions

### Decisions
- **Left eager:** `app-confirm-modal`, `app-auth-modal` (early-session; plan allows leaving eager if latency risk).
- **Left eager:** `app-hero-fab`, `user-msg`, header/loader (not modals).
- Deferred: unit-creator, translation-key, label-creation, add-item, quick-add/edit product, add-equipment, global-specific, supplier, ai-recipe/menu/product, restore-choice.
- Modal internals unchanged; only root instantiation timing.

### Reviewer should scrutinize
- First open of deferred modals loads a chunk — brief delay expected once.
- Confirm/auth remain in initial tree for snappy first use.
- `app.component.spec.ts` may need service stubs if tests inject AppComponent deeply.

### Verify
- Production build clean (budget warning remains but improved).
- Manual: open each deferred modal once; confirm + auth still immediate.

---

## Milestone 5 — App Load Optimization (Defer non-critical eager data)

**Plan:** `plans/289-app-load-optimization.plan.md`
**Milestone:** 5 — Defer non-critical eager data loading
**Status:** Ready for review
**Branch:** `feat/session-20260709`

### Audit findings (defer vs keep eager)
| Service | Verdict | Reason |
|---------|---------|--------|
| EquipmentDataService | **Keep eager** | Used in recipe-builder + venue-form |
| PreparationRegistryService | **Keep eager** | Used in recipe-builder workflow/search |
| VenueDataService | **Defer** | Venues routes + dashboard venues tab only |
| MenuEventDataService | **Defer** | menu-library / menu-intelligence / metadata |
| MenuSectionCategoriesService | **Defer** | menu-intelligence / metadata section manager |

### Files touched
- `base-entity-data.service.ts` — `autoLoad` flag + `ensureLoaded()` / `hasLoaded()`
- `venue-data.service.ts` — `super(ENTITY, false)`
- `menu-event-data.service.ts` / `menu-section-categories.service.ts` — constructor no longer loads; `ensureLoaded()`
- `user.service.ts` — login reload skips deferred services until `hasLoaded()`
- Resolvers: `venues-ensure-loaded`, `menu-events-ensure-loaded`, `menu-section-categories-ensure-loaded`
- `app.routes.ts` — wire resolvers on venues / menu-library / menu-intelligence
- `venue-list` / `venue-form` / `section-category-manager` / `metadata-manager` — `ensureLoaded()` for dashboard-embedded paths
- `venue.resolver.ts` — `ensureLoaded` before getById

### Reviewer should scrutinize
- Login/guest auto-login no longer force-fetches venues/menu-events/section-categories until first visit.
- Dashboard overview should not GET those three collections; opening venues/metadata tabs should.
- Equipment + preparations still load when their services are first constructed (recipe-builder / equipment pages).

### Verify
- `tsc --noEmit`: clean
- Manual Network: cold `/dashboard` — no `VENUE_PROFILES` / `MENU_EVENT_LIST` / `MENU_SECTION_CATEGORIES` until navigation/tab

---

## Milestone 6 — Parallelize PUT /:type replaceAll

**Plan:** `plans/289-app-load-optimization.plan.md`
**Milestone:** 6 — Fix PUT /:type double query round-trip
**Status:** Ready for review

### Files touched
- `server/routes/generic.js` — `Promise.all([deleteMany({ userId }), find({ _id: { $in }, userId: { $ne } })])` then insertMany

### Decisions
- Conflict lookup now excludes `req.user.userId` so it is safe to run in parallel with delete (no race on own docs).
- External contract unchanged (`X-Confirm-Replace`, response `{ ok: true }`).
- Id-conflict reassignment retained.

### Verify
- `node --check server/routes/generic.js`: clean
- Manual: trash empty-all + backup import

### STOP
Plan 289 Milestones 4–6 executed. Awaiting Human/Reviewer mark-complete.
