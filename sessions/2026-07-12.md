# Session 2026-07-12

## Milestone: Brief 5 — Commit-vs-PR Judgment Gate

**Status:** Ready for review (contractor does not mark complete)

### Files touched
- `.claude/commands/ship.md` — Phase 4 commit-vs-PR judgment + summary `PR path` lines
- `AGENTS.md` — `/ship` trigger-table note (PR only when feature-complete)
- `sessions/2026-07-12.md` — this handoff

### Decisions
- Replaced implicit step 5 (`gh pr create` when Human asked) with an explicit **Commit-vs-PR judgment** block; build/review/approval/push conflict/PR-merge fallback unchanged.
- Override phrases checked first (“open a PR” / “just commit” / “checkpoint” / “no PR”).
- Brief path: compare session diff to brief **Done when**; incomplete → checkpoint, no PR, mandatory summary line.
- No-brief path: one single-select question; never assume PR or checkpoint.
- Summary always includes `PR path:` with one of the briefed reasons.

### Reviewer should scrutinize
- Whether push wording (“Push only if Human asked”) still aligns with Done-when “commits and pushes” for mid-feature ships (left untouched per “don’t restructure the rest”).
- That Phase 1–3 / 5–6 and `--skip-review` behavior from Brief 4 are intact.

### Lint / test
- `npx ng build` — PASS
- No `src/app/**` changes; docs/command-only scope.

---

## Milestone: Brief 7 — PR-Check Fix Loop

**Status:** Ready for review (contractor does not mark complete)

### Files touched
- `docs/agent/pr-check-fix-loop.md` — shared tool-neutral procedure (detect/diagnose/classify/fix/push/re-check, 2-round cap)
- `.claude/commands/fix-pr-checks.md` — thin adapter (pointer only)
- `.cursor/commands/fix-pr-checks.md` — thin adapter (pointer only)
- `.claude/commands/ship.md` — post-PR offer to run fix loop (ask, never auto-run)
- `AGENTS.md` — trigger-table row for PR checks failing
- `.claude/commands/commands.md`, `.claude/commands/_index.md` — registry entries for `/fix-pr-checks`
- `sessions/2026-07-12.md` — this handoff

### Decisions
- Check names map to real jobs: `ci` / `security`; step-level classify uses workflow step titles (ESLint, Build, Unit tests, Secret scan (gitleaks), npm audit, Semgrep).
- `[security-scan]` (gitleaks + semgrep) always surfaces; never auto-fix.
- Cursor has **no** ship-equivalent under `.cursor/commands/` — Cursor users invoke `/fix-pr-checks` manually after opening a PR (noted for Reviewer / Human).
- Did not run the live fix loop against PR #150’s 104 `[test]` failures — out of Brief 7 scope (procedure authoring only); Diagnose dry-run confirmed classification as `[test]`.

### Dry-run verification
| Scenario | Result |
| --- | --- |
| Detect on PR #150 | `ci` fail / `security` pass; `gh run view --log-failed` → Unit tests → classified `[test]` |
| `[lint]` local | Deliberate unused-var error detected; `eslint --fix` did not auto-clear (hand-fix path per procedure); temp file removed |
| `[security-scan]` | Procedure requires surface-to-user / no push; **did not** stage a fake secret (avoids polluting history / gitleaks noise) |
| Full push→green lint loop | Not executed end-to-end on a throwaway PR (would need isolated commit/push); procedure + Detect/Diagnose path verified |

### Reviewer should scrutinize
- That adapters stay pointers only (no duplicated step text).
- Ship offer is ask-only after PR open.
- 2-round hard stop + never-weaken-gates language.
- Cursor ship gap acknowledged correctly.

### Lint / test
- `npx ng build` — PASS
- `npm run lint` — 0 errors (51 pre-existing warnings)
- Docs/command-only scope for Brief 7 files
