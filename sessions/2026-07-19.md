# Session — 2026-07-19

## Milestone: Brief 1 — Compaction Pipeline (R1 + R1b)

**Branch:** `chore/compaction-pipeline-r1`

### Files touched
- `scripts/pre-compact-todo-append.sh` — after todo dump, transcript signal-grep appended under `### Unresolved signals detected at compact time`; removed `/tmp/claude-compact-baseline` writer
- `scripts/pre-compact-reminder.sh` — removed baseline-recording; kept systemMessage JSON only; header notes pairing with todo-append
- `.claude/settings.json` — PreCompact hooks: todo-append then reminder (Windows bash invocation unchanged)
- `scripts/context-monitor.sh`, `scripts/context-monitor.py`, `.claude/commands/context-override.md` — deleted
- `CLAUDE.md` — PostToolUse wording: session-manifest only
- `scripts/session-startup.sh` — removed `touch /tmp/claude-context-reset` + comment block
- `AGENTS.md` — Hard rule: compaction /clear preference

### Decisions
- Kept TRANSCRIPT path resolution in todo-append (needed for signal-grep); only the baseline-bytes consumer was removed
- Did not touch `session-manifest-hook.py`, `branch-guard.sh`, `handoff-check.sh`
- Left historical `context-monitor` mentions in `.claude/todo.md` / `todo-archive.md` (completed plan checkboxes); live paths clean

### Reviewer should scrutinize
- PreCompact hook order and timeout (both 5s) under Windows Git bash
- Signal-grep false positives / empty-section behavior when transcript missing
- That deleted stubs are not still referenced from any non-historical hook config

### Lint / test
- N/A for shell/docs-only change; verify: `rg context-monitor scripts/ CLAUDE.md .claude/commands/` → no matches; stub files absent

---

## Milestone: Brief 1 fallout cleanup (dead-code + signal-grep harden)

**Branch:** `chore/compaction-pipeline-r1`

### Files touched
- `.claude/commands/commands.md` — removed `/context-override` registry row
- `.claude/commands/_index.md` — removed `/context-override` entry
- `.claude/config/model-context-windows.json` — deleted (only consumer was retired `context-monitor.py`)
- `scripts/pre-compact-todo-append.sh` — stricter signal-grep anchors + `cut -c1-300` per match
- `.claude/todo.md` — Plan 284 Tasks 4–5 closed as obsolete (Brief 1 superseded context-monitor design)

### Intentionally not touched / not for this commit
- `.claude/.session-state-path` — undeclared residue; leave unstaged
- `docs/session-state-main-1.md` — undeclared residue; leave unstaged
- No other command/index rows beyond `/context-override`

### Decisions
- FAIL match requires non-`/` predecessor so `PASS/FAIL` review tables do not match
- `Verify:` requires colon + whitespace (not `Verify cmd`)
- Matched real tokens in spot-check: `UPGRADE_AVAILABLE`, `ROUTING_DECLINED`, `BLOCKED`, `Verify: …`, standalone `FAIL`

### Reviewer should scrutinize
- Signal-grep regex on Windows Git bash `grep -E`
- Whether orphaned `.claude/config/` dir needs cleanup (file deleted only)
- Commit staging must exclude `.session-state-path` and `docs/session-state-main-1.md`

### Lint / test
- `rg context-override .claude/commands/commands.md .claude/commands/_index.md` → no matches
- `model-context-windows.json` absent
- Spot-check: review-table sample produced zero matches; real signal lines matched and truncated
- Nothing staged (`git diff --cached` empty)

---

## Milestone: Plan persist + brief sync (agent infra)

**Branch:** (current feature/chore — docs/scripts only)

### Files touched
- `scripts/plan-name-similarity.mjs` — name/slug token similarity for `plans/`; short why + excerpt
- `scripts/plan-write-guard.sh` — PreToolUse: block new similar plan creates unless `.claude/.plan-write-ack`
- `.claude/settings.json` — wired plan-write-guard after branch-guard
- `.claude/skills/save-plan/SKILL.md` — detect pasted plans; similarity gate; Phase 4 brief sync
- `.cursor/rules/save-plan-must-use-skill.mdc` — alwaysApply Cursor parity
- `AGENTS.md`, `.cursorrules`, `.claude/commands/brief.md`, `docs/agent/job-validation.md`
- `docs/brain/gotchas.md` — pasted plans that never hit `plans/`
- `.gitignore` — `.claude/.plan-write-ack`

### Decisions
- Similarity = plan **name/slug tokens** only (threshold 0.8); prompt rewrite/new/cancel **only** on hits
- Claude: hook enforce; Cursor: always-on rule + same script (no separate Cursor hook file)
- Mid-flight tasks must update parent plan Atomic Sub-tasks + `.claude/todo.md`

### Reviewer should scrutinize
- False-positive/negative rate of name similarity on real plan titles
- PreToolUse stdin timeout fail-open behavior under Cursor vs Claude
- Ack file lifecycle (gitignored)

### Lint / test
- `node scripts/plan-name-similarity.mjs --name="Project Memory Bank"` → hits 290
- `node scripts/plan-name-similarity.mjs --name="Totally Unique Zebra Widget"` → no hits
- plan-write-guard smoke: non-plan allow; similar new deny; ack allow; existing edit allow
