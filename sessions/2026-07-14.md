# Session — 2026-07-14

## Milestone: Plan 289 — Milestone 5 (Defer non-critical eager data loading)

**Plan:** `plans/289-app-load-optimization.plan.md`  
**Branch:** `feat/defer-eager-data-m5`  
**Status:** READY FOR REVIEW — then `/ship` per Human instruction  
**Scope:** client data-load timing only — no demo-loader / backup / eager core registries

### Prior state (already on main from `a2d7ed2`)
Venue / menu-event / menu-section-categories already deferred with `ensureLoaded()` + route resolvers. Previous audit left Equipment + Preparations eager.

### Fresh audit (5.1)

| Service | Verdict | Reason |
|---------|---------|--------|
| VenueDataService | **Already deferred** | Venues routes + dashboard venues tab (`ensureLoaded` in list/form) |
| MenuEventDataService | **Already deferred** | menu-library / menu-intelligence / metadata |
| MenuSectionCategoriesService | **Already deferred** | menu-intelligence + section-category-manager |
| EquipmentDataService | **Defer (this session)** | Not read on dashboard overview; used by equipment routes, recipe-builder, venue-form. Inventory list had a dead inject (no signal reads). |
| PreparationRegistryService | **Defer (this session)** | Not on overview; used by recipe-builder + dashboard metadata prep manager |

**Kept eager (untouched):** Recipe, Dish, Product, Supplier, UnitRegistry, MetadataRegistry.

### Files touched
- `equipment-data.service.ts` — `super(ENTITY, false)`
- `preparation-registry.service.ts` — `loaded_` / `ensureLoaded()` / `hasLoaded()`; constructor no longer loads
- `equipment-ensure-loaded.resolver.ts` — **new**
- `preparations-ensure-loaded.resolver.ts` — **new**
- `equipment.resolver.ts` — `ensureLoaded` before getById
- `app.routes.ts` — wire equipment + preparations resolvers on owning routes (equipment on form routes only under venues, not venues list)
- `user.service.ts` — login reload skips equipment + preparations until `hasLoaded()`
- `venue-form.component.ts` — `equipmentData.ensureLoaded()` (dashboard add-venue path)
- `preparation-category-manager.component.ts` — `ensureLoaded()` on init (dashboard metadata tab)
- `sessions/2026-07-14.md` — this handoff

### Decisions
1. Reversed prior “keep equipment/preparations eager” choice: recipe-builder need is solved by route resolvers, not bootstrap GETs.
2. Dashboard overview stays free of those GETs; venues/metadata tabs still hydrate via component `ensureLoaded()`.
3. Did not touch demo-loader or backup (`reloadFromStorage` still works).

### Lint / build
- `ng build`: **PASS** (existing initial-bundle budget warning unchanged)
- `ng lint` target missing; scoped `npx eslint` on touched files: **PASS**

### Reviewer should scrutinize
- Cold `/dashboard` Network: no `EQUIPMENT_LIST` / `KITCHEN_PREPARATIONS` / `VENUE_PROFILES` / `MENU_EVENT_LIST` / `MENU_SECTION_CATEGORIES` until navigation or tab open
- Recipe-builder first visit populates equipment + prep search
- Equipment list + venue form equipment picker still populate
- Login/guest reload no longer force-fetches deferred collections

### Brain (written on ship approve — expanded)
- `docs/brain/patterns/defer-singleton-data-ensureLoaded.md` — Problem / Solution / When
- `docs/brain/gotchas.md` — Login reload bypasses deferred constructor load
- `notes/brain-capture-quality-brief.md` — brief for improving agent brain recommendations

### Explicitly not done
- Milestone 6
- Marking `.claude/todo.md` items `[x]` (Human/Reviewer)
