# Session 2026-07-20

## Plan 292 — Milestone 1 — Archive script + volume rotation

**Status:** Ready for review (Contractor STOP after one milestone)

### Files touched
- `plans/292-todo-archive-volume-rotation.plan.md` (new — save-plan; similarity: none)
- `.claude/todo.md` — ledger section for Plan 292
- `scripts/todo-archive.mjs` (new)
- `.claude/todo-archive/README.md` (new)

### Decisions
- Volumes live under `.claude/todo-archive/NNN.md`, max 300 lines, roll on append.
- Skip sections containing `(deferred)` / `(skipped)` / `[~]`.
- Footer detection for todo.md only matches `## Plan Index` or `## Done` + stub line
  (“Completed entries are in”) — so legacy archive mid-file `## Done` markers are not
  treated as footers during `--migrate`.
- Plan Index slim + live migrate deferred to M3 (per plan).

### Verify
- `node scripts/todo-archive.mjs --dry-run` → 9 all-`[x]` candidates (3 blocked/deferred skipped)
- `node scripts/todo-archive.mjs --migrate --migrate-only --dry-run` → 212 sections → 9 volumes, each ≤ 300 lines

### Reviewer should scrutinize
- Volume roll edge case: single section larger than 300 lines still written (never dropped)
- Dry-run archive targets `001.md` until migrate creates volumes (expected)
- Encoding mojibake in existing headings (`â€”`) left untouched — not this milestone

### Lint / tests
- Tooling-only (`.mjs` + markdown). No `ng lint` surface.

---

## Plan 292 — Milestone 2 — Wire commands + similarity last-2

**Status:** Ready for review (Contractor STOP after one milestone)

### Files touched
- `.claude/commands/ship.md` — archive via `node scripts/todo-archive.mjs`
- `.claude/commands/done.md` — same
- `.claude/commands/sweep-stale-todos.md` — script + unverifiable gate before run
- `.claude/commands/auto-solve.md` — script replaces hand-append
- `scripts/plan-name-similarity.mjs` — scans last two `todo-archive/NNN.md` volumes
- `docs/agent/job-validation.md` — Todo archive volumes section

### Decisions
- Sweep still git-verifies first; if any candidate is unverifiable, do not run the script until Human confirms.
- Archive hits tagged `[archive]` in similarity stdout; JSON includes `archiveVolumesScanned`.

### Verify
- Empty volumes: similarity still returns plans/ hits for "AI Phase 2 Products" (plan file exists)
- Temp `001.md` with Plan 287 heading → second hit `.claude/todo-archive/001.md [archive]`; temp file removed

### Reviewer should scrutinize
- auto-solve still requires literal `"approve"` before running the archive script
- Similarity archive scan is headings-only (not full checkbox bodies) — intentional for token budget

---

## Plan 292 � Milestone 3 � Migrate + slim todo.md

**Status:** Ready for review (Contractor STOP after one milestone)

### Files touched
- .claude/todo-archive/001.md � 09.md � migrated from legacy + newly archived sections
- .claude/todo-archive/INDEX.md � 82 Done Plan Index rows
- .claude/todo-archive.md � stub pointing at volumes
- .claude/todo-archive.legacy.md � renamed monolith (pre-migration backup)
- .claude/todo.md � orphans hoisted above footer; Done index rows removed; all-`[x]` archived
- `AGENTS.md` � one-line pointer to volume archive

### Decisions
- Sections that lived *below* Plan Index (mobile audit 276�283, etc.) were moved back into Active Tasks so the archive script can see them.
- Only remaining all-`[x]` in todo.md is Plan 074 (contains `(deferred)` � correctly kept).
- Slim index still ~86 non-Done rows � expected; Done history is in INDEX.md + volumes.
- Similarity against "AI Phase 2 Products" hits `plans/287-�` only (Plan 287 is in volume 001, not last-two 008/009) � correct per last-2 rule.
- `## Workflow Evolution` block rode along inside archived Plan 276 Design System section into `009.md` (preserved, not dropped).

### Verify
- Volumes 001�009 each = 300 lines
- `todo.md` ~487 lines (was ~712); dry-run archive: nothing left to move
- Stub + INDEX + README present; legacy bak present

### Reviewer should scrutinize
- Whether `todo-archive.legacy.md` should be deleted after Human review
- Plan Index still lists some Active rows that may be stale vs real open sections � out of M3 scope

---

## Plan 294 — Milestone 1 — Informed by reuse-tracking convention

**Branch:** `chore/brain-reuse-instrumentation` (from `main`; `chore/brain-auto-write-default` WIP was stashed on that branch)

**Status:** Ready for review (Contractor STOP after one milestone)

### Milestone ID
Plan 294 — M1

### Files touched
- `docs/agent/brain-capture.md` — new **Reuse tracking (session logs)** section (after Extraction procedure, before Usefulness gate)
- `docs/brain/index.md` — Maintenance: one-line pointer to `Informed by:` + audit grep
- `sessions/2026-07-20.md` — this handoff only (no backfill of past `Informed by:` lines)

### Decisions
- Forward-only convention; did not rewrite older session logs (per plan).
- Left Plan 294 todo / plan checkboxes `[ ]` — Human validation required before mark.
- Cleared leaked ADR 0006 / Plan 295 working-tree edits from this branch so M1 stays scoped.

### Reviewer should scrutinize
- Placement and wording of the new section in `brain-capture.md` (cite-only-when-choice-changed bar vs blanket citation habit).
- Whether the `index.md` Maintenance line is discoverable enough for a future value audit.

### Lint / test status
- Doc-only; no Angular/product code changed.
- `npm run lint` — 0 errors (51 pre-existing `any` warnings in specs only); `npx ng lint` has no lint target in this project.
- Product build not re-run (unchanged surface).

### STOP
M1 complete. Awaiting Human review before M2.

---

## Plan 296 — Milestone 1 — Blank inventory list rows (paint fix)

**Status:** Ready for review (Contractor STOP after one milestone)

**Parent plan:** `plans/296-fix-blank-inventory-list-rows.plan.md`

### Files touched
- `plans/296-fix-blank-inventory-list-rows.plan.md` (new — save-plan; similarity: none)
- `.claude/todo.md` — ledger section for Plan 296
- `src/app/shared/list-shell/list-shell.component.scss` — moved `.table-area` `backdrop-filter` onto `::before`; `.table-header` / `.table-body` stacked above (`z-index: 1`)
- `src/styles.scss` — `.c-list-body-cell`: `color: var(--color-text-main)` + `isolation: isolate`

### Decisions
- Kept glass look; blur no longer owns the scrolled cell compositing root (nutrition tooltip was forcing a rebuild that “woke” blank text).
- Did not rewrite `display: contents` rows (out of scope).
- CSS landed on current branch `chore/brain-reuse-instrumentation` (working tree already had Plan 294 WIP; SCSS files do not overlap). Prefer splitting to `fix/blank-inventory-list-rows` before commit if Human wants a clean PR.

### Reviewer should scrutinize
- Cold open `/inventory/list`: every visible row shows name/unit/price **without** hovering nutrition.
- Nutrition tooltip still positions/opens correctly.
- Glass look on list-shell (recipe book / suppliers) still acceptable.
- If blank rows persist, next escalation is nutrition-badge tooltip `backdrop-filter` / row layout — not done in this milestone.

### Lint / test status
- SCSS-only; `npm run lint` covers `ts`/`html` only — no new surface.
- `npx ng lint` has no lint target in this project.

### STOP
M1 complete. Awaiting Human validation before marking todos `[x]`.

---

## Plan 296 — Milestone 2 — Remove table-area blur (M1 fallout)

**Status:** Ready for review (Contractor STOP)

**Parent plan:** `plans/296-fix-blank-inventory-list-rows.plan.md`

### Why
Human confirmed M1 CSS is present in DevTools (`::before` + `isolation` + `color`) but blank rows remain. Screenshot still shows partial rows (checkbox + ₪ only) and a center glass glow consistent with `::before` blur.

### Files touched
- `plans/296-fix-blank-inventory-list-rows.plan.md` — appended M2 decisions + atomic tasks
- `.claude/todo.md` — M2a/M2b ledger rows
- `src/app/shared/list-shell/list-shell.component.scss` — removed `.table-area::before` and z-index stack; **no** `backdrop-filter` on `.table-area`
- `src/styles.scss` — dropped `isolation: isolate`; added `transform: translateZ(0)` on `.c-list-body-cell`; kept `color`

### Reviewer should scrutinize
- Hard-refresh `/inventory/list`: bottom rows must show name/unit/price/icons without nutrition hover.
- Center circular glow should be gone.
- Glass look is flatter on the table shell (intentional — fill only, no blur).

### STOP
M2 complete. Awaiting Human validation.

---

## Plan 296 — Milestone 3 — Missing Lucide icon aborted CD (browse-verified)

**Status:** Ready for review (Contractor STOP)

**Parent plan:** `plans/296-fix-blank-inventory-list-rows.plan.md`

### Root cause (verified in headless browse)
- Signal had all 257 product names; DOM rows were empty until `ng.applyChanges`.
- Console flooded with: `The "more-vertical" icon has not been provided` from
  `RowActionsMenuComponent` (every row). Exception aborted `@for` CD mid-list.
- Nutrition hover started another CD pass → appeared to “wake” a few rows.

### Fix
- `src/app/app.config.ts` — register Lucide `MoreVertical` in `LucideAngularModule.pick`
  (already present in `src/testing/test-lucide-icons.ts`, missing from app providers).

### Also kept from M2
- No `backdrop-filter` on `.table-area` content root
- `.c-list-body-cell` explicit `color` + `translateZ(0)`

### Browse verify
- Cold `/inventory/list`: `{ filled: 257, empty: 0 }` for all `.product-grid-row` name cells
- Screenshot: `.claude/reports/inventory-after-icon-fix.png`

### STOP
Awaiting Human validation (`done` to mark Plan 296 todos).

---

## Plan 296 — M4 — Revert paint-theory CSS

**Status:** Done (awaiting Human mark)

### Files
- Reverted `list-shell.component.scss` `.table-area` backdrop-filter
- Reverted `styles.scss` `.c-list-body-cell` color + translateZ
- Left only `MoreVertical` in `app.config.ts`

### Diff now
Product code change is `app.config.ts` only (+2 lines).

---

## Plan 294 — Milestones 2–4 (batch per Human: execute all, report at end)

**Branch:** `chore/brain-reuse-instrumentation`
**Status:** Ready for review (all milestones complete; Human validation pending)

### Milestone IDs
Plan 294 — M2, M3, M4 (M1 already on branch HEAD)

### Files touched
- M2: `docs/brain/patterns/gemini-backend-proxy.md` (strengthened), `signals-only-state.md` (flag-for-removal), `tombstone-soft-delete.md` (keep-as-is) — gate comments only + optional strengthen; none deleted
- M3: `docs/brain/gotchas.md` (~150-line / ~10-entry split threshold); `docs/agent/brain-capture.md` (bypass-escalation / structural-fix signal policy)
- M4: `docs/brain/decisions/0004-full-draft-brain-proposals.md` — `## Review log` dated 2026-07-20

### Decisions
- Gemini: named `environment.ts` / client-SDK near-miss → **strengthened**
- Signals: AGENTS + conventions already carry the rule → **flag-for-removal** (Human sign-off required; not deleted)
- Tombstone: why (solo-dev safety net) not in AGENTS hard rules → **keep-as-is**; light when-not for ephemeral UI state
- M3/M4: verbatim plan text; ADR 0004 Review log notes `none durable` still unexercised in logged sessions

### Reviewer should scrutinize
- **Removal recommendation (Human):** delete `docs/brain/patterns/signals-only-state.md`? Gate says flag-for-removal.
- Gemini strengthen wording — enough judgment vs restating `standards-security.md`?
- Bypass-escalation wiki-link title matches gotcha heading `Existing save-plan mitigations still let a plan skip plans/`
- Unrelated dirty tree (inventory list-shell / Plan 296 / todo.md) — left untouched; not part of 294

### Lint / test status
- Doc-only Plan 294 surface. Unrelated Angular SCSS dirty files not linted as part of this job.

### STOP
Plan 294 M1–M4 complete on disk. Awaiting Human validation before marking todos `[x]`.
