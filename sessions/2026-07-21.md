# Session — 2026-07-21

## Plan 291 — Milestone 1 — Ledger integrity checker

**Branch:** `chore/ship-fast-lane`

### Done
- Restored missing `plans/291-plan-persistence-brief-sync-hardening.plan.md` from commit `a627cdd` (required before execute; audit note).
- Added `scripts/plan-ledger-check.mjs`:
  - Parses `.claude/todo.md` `### Plan NNN` sections for every `plans/….plan.md` ref
  - Parses `.claude/sessions/*/brief.md` `## Parent plan` lines (skips `none` / ad-hoc)
  - Resolves refs by exact path, then recursive basename under `plans/` (covers `1-100/`, `100-200/`)
  - Advisory: duplicate NNN prefixes + non-canonical stray filenames
  - Exit **1** on any missing ref; exit **0** with warnings only for advisory findings
  - `--json` for machine-readable output

### Verify
```
node scripts/plan-ledger-check.mjs  → exit 1
```
Hard failures today:
1. `plans/285-ai-menu-phase1.plan.md` (expected ghost — M7 backfill)
2. `plans/297-sidebar-filter-panel-unification.plan.md` (extra ghost; not in Plan 291 scope — flag for Human)

Advisory: 19 duplicate-NNN groups, 4 stray names (as designed).

### Decisions
- Basename fallback under `plans/` so ledger paths like `` `plans/089-….plan.md` `` resolve when the file lives in `plans/1-100/`.
- Collect all plan-path refs *inside* each Plan section (not only the heading backtick), so the M7 bullet referencing 285 surfaces as a hard failure before M7 runs.

### Reviewer should scrutinize
- Whether child-plan naming (`005-1-…` sharing NNN `005`) should stay in the duplicate report (spec says yes; noisy).
- Plan 297 missing file — reconstruct, prune from todo, or leave until a follow-up.
- Wiring into pre-commit is **M2** — intentionally not done yet (M1 standalone first, per risk note).

### Lint / test
- No Angular / SCSS touched. `ng lint` not run (tooling-only `.mjs`).
- Script smoke: plain + `--json` exercised.

### Stop
M1 complete. Awaiting Human validation before M2.

---

## Plan 291 — Milestone 2 — Wire ledger check into enforcement

**Branch:** `chore/ship-fast-lane`

### Done
- Marked M1 `[x]` in `.claude/todo.md` + plan Atomic Sub-tasks (Human `done`).
- `.husky/pre-commit` — appended `node scripts/plan-ledger-check.mjs` after the three existing `pre-commit-*.mjs` calls.
- `.claude/commands/ship.md` Phase 1 — added `node scripts/plan-ledger-check.mjs` beside `ng build`; hard-fail language updated (duplicates/strays remain advisory).

### Verify
- Hook line present in `.husky/pre-commit`.
- `node scripts/plan-ledger-check.mjs` still exits 1 (285 + 297 ghosts).
- Empty-commit probe used to confirm husky blocks (see below).

### Reviewer / Human — commit gate implication
Wiring the hard gate **now** means **all commits fail** until missing plan files are resolved:
1. `plans/285-ai-menu-phase1.plan.md` → Plan 291 **M7**
2. `plans/297-sidebar-filter-panel-unification.plan.md` → out of plan scope; reconstruct or drop the todo heading ref

Recommend: validate M2, then **M7 next** (and fix 297) before `/ship`.

### Lint / test
- Docs/hook only — no Angular. No `ng lint`.

### Stop
M2 complete. Human validated (`done`) — M2 marked `[x]` in todo + plan.

Next open: M3–M7. Commits still blocked until 285 (+ 297) plan files exist.

---

## Plan 291 — Milestones 3–7 (batch, Human-requested)

**Branch:** `chore/ship-fast-lane`

### M3 — `_v[N]` convention fork closed
- `.claude/commands/plan.md`, `feat.md`, `review-it.md`, `_index.md` → `plans/NNN-slug.plan.md`
- `docs/agent/workflow-map.md` inconsistency notes updated (fork / ledger / brief race / ghost 285)

### M4 — brief-detection Plan Contract gate
- `.claude/skills/brief-detection/SKILL.md` — shape check first → "Detected Plan Contract — routing to save-plan"; a/b/c only for genuine briefs

### M5 — `/brief` dangling parent STOP
- `.claude/commands/brief.md` Step 2a + 2b — disk check; offer `run save-plan now | proceed ad-hoc (parent plan: none) | cancel`

### M6 — save-plan origin/main NNN
- `.claude/skills/save-plan/SKILL.md` Phase 1 numbering — `git ls-tree -r origin/main --name-only -- plans/` + take higher max

### M7 — ghost Plan 285 restored
- `plans/285-ai-menu-phase1.plan.md` reconstructed (all `[x]`, provenance note)
- `node scripts/plan-ledger-check.mjs` → **exit 0** (advisory duplicates/strays only)
- Note: Plan 297 ref was already pruned from todo by concurrent session; no longer a hard fail

### Stop
M3–M7 implemented. Ledger checkboxes left `[ ]` for Human validation. Commits should succeed again.
