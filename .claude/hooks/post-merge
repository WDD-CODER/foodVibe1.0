#!/bin/sh
# MemPalace auto-mine: runs after every git pull / branch merge.
# No file-type guard - merges always bring meaningful changes.

node "C:/Users/danwe/embed-runner.js" >> "C:/Users/danwe/embed-runner.log" 2>&1 &
