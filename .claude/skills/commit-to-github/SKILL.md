---
name: commit-to-github
description: DEPRECATED — use git-agent instead.
---

# DEPRECATED — Use git-agent instead

This skill has been replaced by `.claude/agents/git-agent.md`.

All git operations (commit, push, PR, merge) are handled by the git-agent.
