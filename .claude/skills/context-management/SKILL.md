# context-management
Use the host agent's native context management (compact / handoff).
Custom 40/60/70% threshold hooks are retired.
For mid-task persistence, update `.claude/session-state.md` or run `/ship`.
Do not reintroduce percentage-based context monitors.
