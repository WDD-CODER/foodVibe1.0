# Evidence Log: cssLayer — 2026-04-03
Skill commit: 04cbdf8 | Score: 100.0

## TC-001: New Component SCSS
### B1 — Scans src/styles.scss for existing .c-* engine classes before writing new styles
- STATUS: PASS
- EVIDENCE: SKILL.md line 23: "Engine Search: Scan `src/styles.scss` for existing `.c-*` engine classes that can be composed before writing new styles."
- REASONING: Phase 1 mandates this scan as its first action before any writing

### B2 — Checks src/app/shared/ for composable patterns before writing markup
- STATUS: PASS
- EVIDENCE: SKILL.md line 27: "Shared UI Check: Scan `src/app/shared/` for composable patterns before writing new markup."
- REASONING: Phase 1 Step 3 mandates this check

### B3 — Applies Five-Group Vertical Rhythm with blank-line separation
- STATUS: PASS
- EVIDENCE: SKILL.md line 33: "Apply the **Five-Group Vertical Rhythm** in every selector — in this order, each group separated by a blank line"
- REASONING: Phase 2 mandates the full rhythm in every selector

### B4 — Uses logical properties (padding-inline, padding-block) not physical
- STATUS: PASS
- EVIDENCE: SKILL.md line 16: "Logical properties only: `margin-inline`, `padding-block` — no physical directional values"
- REASONING: Rule explicitly prohibits physical directional properties

### B5 — Does NOT define any .c-* class inside the component .scss file
- STATUS: PASS
- EVIDENCE: SKILL.md line 12: "`.c-*` engine classes belong **only** in `src/styles.scss` — never inside a component `.scss`"
- REASONING: Rule explicitly restricts .c-* to src/styles.scss only

### AP1–AP4 — all CLEAR
- EVIDENCE: Lines 23, 16, 33, 15 collectively prevent all four anti-patterns

---

## TC-002: Component File Contains .c-* Classes
### B1 — Identifies .c-* in component file as violation
- STATUS: PASS
- EVIDENCE: SKILL.md line 12: "`.c-*` engine classes belong **only** in `src/styles.scss` — never inside a component `.scss`"

### B2 — Moves definitions to src/styles.scss
- STATUS: PASS
- EVIDENCE: SKILL.md line 14: "If a `.c-*` is found in a component file → move to `src/styles.scss` before proceeding"

### B3 — Removes definitions from component file
- STATUS: PASS
- EVIDENCE: SKILL.md line 14: "move to `src/styles.scss`" (move = remove from origin) + line 12: "never inside a component `.scss`"

### B4 — Explains why: Angular view encapsulation breaks reuse
- STATUS: PASS
- EVIDENCE: SKILL.md line 13: "Angular view encapsulation will scope `.c-*` defined in components, breaking cross-component reuse"

### AP1–AP3 — all CLEAR
- EVIDENCE: Lines 14 and 12 collectively prevent all three anti-patterns

---

## TC-003: Five-Group Rhythm — Ambiguous Properties
### B1 — display, gap, z-index → Group 1 (Layout)
- STATUS: PASS
- EVIDENCE: SKILL.md line 35: "1. **Layout** — `display`, `flex`, `grid`, `position`, `gap`, `z-index`"

### B2 — width → Group 2 (Dimensions)
- STATUS: PASS
- EVIDENCE: SKILL.md line 36: "2. **Dimensions** — `width`, `height`, `aspect-ratio`"

### B3 — color → Group 3 (Content)
- STATUS: PASS
- EVIDENCE: SKILL.md line 37: "3. **Content** — typography, colors, `content`"

### B4 — border-radius AND padding-block → Group 4 (Structure)
- STATUS: PASS
- EVIDENCE: SKILL.md line 38: "4. **Structure** — `margin`, `padding`, `border`, `border-radius`"

### B5 — transition AND animation → Group 5 (Effects)
- STATUS: PASS
- EVIDENCE: SKILL.md line 39: "5. **Effects** — `transition`, `animation`, `shadow`, `opacity`"

### B6 — Exactly one blank line between groups
- STATUS: PASS
- EVIDENCE: SKILL.md line 33: "each group separated by a blank line"

### AP1–AP4 — all CLEAR
- EVIDENCE: Lines 35, 38, 35, 39 explicitly prevent all four wrong-placement anti-patterns

---

## TC-004: Repeated Styles Across Three Components
### B1 — Recognizes Phase 3 trigger (3 > 2 components)
- STATUS: PASS
- EVIDENCE: SKILL.md line 47: "Only invoke if the same styles are repeated across more than two components."

### B2 — Proposes .c-* in src/styles.scss
- STATUS: PASS
- EVIDENCE: SKILL.md line 49: "Propose a new `.c-*` engine class for `src/styles.scss`"

### B3 — Names, defines, registers the class
- STATUS: PASS
- EVIDENCE: SKILL.md line 49: "name it, define it, register it"

### B4 — Replaces in all three component files
- STATUS: PASS
- EVIDENCE: SKILL.md line 49: "replace the repeated styles in each affected component file"

### AP1–AP3 — all CLEAR
- EVIDENCE: Lines 47, 12+49, 47 collectively prevent all three anti-patterns

---

## TC-005: Trigger Boundary — Should NOT Activate
### B1 — Skill does NOT activate for .ts file
- STATUS: PASS
- EVIDENCE: SKILL.md line 9: "Trigger: Before creating or editing any `.scss` or `.css` file in `src/`."

### B2 — No CSS guidance given
- STATUS: PASS
- EVIDENCE: SKILL.md line 9 (trigger constraint prevents activation)

### B3 — Response focuses on TypeScript
- STATUS: PASS
- EVIDENCE: SKILL.md line 9 (non-activation allows TypeScript focus)

### AP1–AP2 — all CLEAR
- EVIDENCE: Line 9 trigger constraint prevents both anti-patterns

---

## TC-006: Logical Properties — Physical Direction Rejection
### B1 — Uses padding-inline: 16px for horizontal spacing
- STATUS: PASS
- EVIDENCE: SKILL.md line 16: "Logical properties only: `margin-inline`, `padding-block` — no physical directional values"
- REASONING: "Logical properties only" covers all logical forms including padding-inline

### B2 — Uses padding-block-start: 8px for top spacing
- STATUS: PASS
- EVIDENCE: SKILL.md line 16: "Logical properties only... no physical directional values"
- REASONING: padding-block-start is the logical form of padding-top

### B3 — Uses padding-block-end: 0 or omits if default
- STATUS: PASS
- EVIDENCE: SKILL.md line 16: same rule

### B4 — Places border in Group 4 (Structure)
- STATUS: PASS
- EVIDENCE: SKILL.md line 38: "4. **Structure** — `margin`, `padding`, `border`, `border-radius`"

### AP1–AP4 — all CLEAR
- EVIDENCE: Line 16 prevents physical directional properties; line 38 prevents wrong border group placement

---

## TC-007: Phase 1 — Compose Existing Engine Class Before Writing New Styles
### B1 — Scans src/styles.scss before writing any styles
- STATUS: PASS
- EVIDENCE: SKILL.md line 23: "Engine Search: Scan `src/styles.scss` for existing `.c-*` engine classes that can be composed before writing new styles."

### B2 — If matching engine class found, composes it instead of rewriting
- STATUS: PASS
- EVIDENCE: SKILL.md line 23: "...classes that **can be composed** before writing new styles."
- REASONING: "can be composed before writing new styles" = compose them (use them) instead of writing new; the scan's stated purpose is finding composable classes

### B3 — If no engine class found, writes new styles with Five-Group Rhythm
- STATUS: PASS
- EVIDENCE: SKILL.md lines 31–41: Phase 2 Structural Authoring applies when Phase 1 finds nothing composable

### B4 — Does NOT define .c-* inside component .scss regardless of outcome
- STATUS: PASS
- EVIDENCE: SKILL.md line 12: "`.c-*` engine classes belong **only** in `src/styles.scss` — never inside a component `.scss`"

### AP1–AP3 — all CLEAR
- EVIDENCE: Lines 23, 12, 27 collectively prevent all three anti-patterns

---

## TC-008: Inline Styles — Static Value Must Go in SCSS
### B1 — height: 240px in SCSS Group 2 (Dimensions), not inline
- STATUS: PASS
- EVIDENCE: SKILL.md line 15: "No inline styles unless the value is dynamic/runtime" + line 36: "2. **Dimensions** — `width`, `height`, `aspect-ratio`"

### B2 — background-color: #1a1a2e in SCSS Group 3 (Content), not inline
- STATUS: PASS
- EVIDENCE: SKILL.md line 15: "No inline styles unless the value is dynamic/runtime" + line 37: "3. **Content** — typography, colors, `content`"

### B3 — Five-Group Rhythm with height in Group 2 and background-color in Group 3
- STATUS: PASS
- EVIDENCE: SKILL.md line 33: "Apply the Five-Group Vertical Rhythm in every selector" + lines 36, 37

### AP1–AP3 — all CLEAR
- EVIDENCE: SKILL.md line 15 prevents all three static inline style anti-patterns (style attribute, [style.height] with static value, [style.backgroundColor] with static value)

---

## TC-009: Responsive Breakpoints — Must Follow Token Definitions
### B1 — Uses project token breakpoints, not hardcoded pixel values
- STATUS: PASS
- EVIDENCE: SKILL.md line 17: "Responsive breakpoints must follow project token definitions"

### B2 — Places breakpoint rules after base selector, not before
- STATUS: PASS
- EVIDENCE: SKILL.md line 41: "Place responsive breakpoint blocks after the base selector — never before the base styles."
- REASONING: Rule explicitly mandates placement order

### B3 — Applies Five-Group Rhythm inside responsive block
- STATUS: PASS
- EVIDENCE: SKILL.md line 33: "Apply the Five-Group Vertical Rhythm in every selector"
- REASONING: "in every selector" includes selectors inside responsive blocks

### AP1–AP3 — all CLEAR
- EVIDENCE: Line 17 prevents hardcoded breakpoint values; line 35 covers grid-template-columns under Layout (grid)

---

## TC-010: Group Placement — z-index (Layout) vs opacity (Effects)
### B1 — z-index: 1000 → Group 1 (Layout)
- STATUS: PASS
- EVIDENCE: SKILL.md line 35: "1. **Layout** — `display`, `flex`, `grid`, `position`, `gap`, `z-index`"

### B2 — opacity: 0.85 → Group 5 (Effects)
- STATUS: PASS
- EVIDENCE: SKILL.md line 39: "5. **Effects** — `transition`, `animation`, `shadow`, `opacity`"

### B3 — transition: opacity 0.2s ease → Group 5 (Effects)
- STATUS: PASS
- EVIDENCE: SKILL.md line 39: "5. **Effects** — `transition`, `animation`, `shadow`, `opacity`"

### B4 — width: 100vw and height: 100vh → Group 2 (Dimensions)
- STATUS: PASS
- EVIDENCE: SKILL.md line 36: "2. **Dimensions** — `width`, `height`, `aspect-ratio`"

### AP1–AP3 — all CLEAR
- EVIDENCE: Lines 35 and 39 explicitly put z-index and opacity in different groups, preventing all three wrong-placement anti-patterns
