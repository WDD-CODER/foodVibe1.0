# Evidence Log: cssLayer — 2026-04-06
Skill commit: 22c7edf | exec_score: 38.6 | behavior_score: 30.0 | agent_score: 28.8 | final_score: 97.4

## Evaluator Agent Output

EVALUATION RESULTS
==================
exec_score:             38.6
behavior_score:         30.0
agent_score:            28.8
final_score:            97.4
agent_behaviors_total:  25
agent_behaviors_passed: 24
==================

## test-runner.sh Output (raw, pre-tier-adjustment)

skill_score: 67.5 (55/57 checks passed — 2 FAILs on GREP: "padding-inline")

## Behavior Checks (12/12 passed)

TC-001 RUN_AGENT: "Add styles: display flex, color red, padding 10px, width 200px, transition all 0.3s"
- OUTPUT-GREP-BEFORE: "display" BEFORE "color" → PASS (display in Layout, color in Content)
- OUTPUT-GREP-BEFORE: "width" BEFORE "transition" → PASS (width in Dimensions, transition in Effects)
- OUTPUT-GREP-NOT: "padding-left" → PASS (used padding-inline instead)
- OUTPUT-GREP-NOT: "padding-right" → PASS (used padding-inline instead)

TC-003 RUN_AGENT: "Style a sidebar: flex column, gap 1rem, width 280px, z-index 10, opacity 0.9, transition width 0.3s"
- OUTPUT-GREP-BEFORE: "z-index" BEFORE "opacity" → PASS (z-index in Layout Group 1, opacity in Effects Group 5)
- OUTPUT-GREP-NOT: "margin-left" → PASS

TC-006 RUN_AGENT: "Add 16px horizontal spacing and 8px top spacing to a card component"
- OUTPUT-GREP: "padding-inline" → PASS (logical property used for horizontal spacing)
- OUTPUT-GREP-NOT: "padding-left" → PASS
- OUTPUT-GREP-NOT: "padding-right" → PASS

TC-011 RUN_AGENT: "Add styles: overflow hidden, width 280px, padding-inline 1rem, border 1px solid, transition width 0.3s"
- OUTPUT-GREP-BEFORE: "overflow" BEFORE "width" → PASS (overflow in Layout, width in Dimensions)
- OUTPUT-GREP-BEFORE: "width" BEFORE "padding" → PASS (Dimensions before Structure)
- OUTPUT-GREP-BEFORE: "padding" BEFORE "transition" → PASS (Structure before Effects)

## Agent-Evaluated Behaviors — Full Evidence

### TC-001: New Component SCSS

- [x] The Five-Group order is presented as strict/mandatory sequence
  EVIDENCE: SKILL.md line 33: "Apply the **Five-Group Vertical Rhythm** in every selector — in this order, each group separated by a blank line"
  REASONING: "in this order" makes the sequence mandatory, not optional.

- [x] Phase 1 clearly requires scanning `src/styles.scss` BEFORE writing any new styles
  EVIDENCE: SKILL.md line 23: "**Engine Search:** Scan `src/styles.scss` for existing `.c-*` engine classes that can be composed before writing new styles."
  REASONING: "before writing new styles" explicitly orders Phase 1 scan before writing.

### TC-002: Component File Contains .c-* Classes

- [x] The skill is specific enough that a developer finding .c-* in a component file would know to MOVE them to src/styles.scss
  EVIDENCE: SKILL.md line 14: "If a `.c-*` is found in a component file → move to `src/styles.scss` before proceeding"
  REASONING: "move" is the explicit instruction — not rename, not delete.

- [x] The explanation of WHY (Angular view encapsulation scopes .c-* in components) is present and clear
  EVIDENCE: SKILL.md line 13: "Angular view encapsulation will scope `.c-*` defined in components, breaking cross-component reuse"
  REASONING: Explains the mechanism and consequence clearly.

### TC-003: Five-Group Rhythm — Ambiguous Properties

- [x] gap is explicitly listed under Group 1 (Layout) — not under Structure or Dimensions
  EVIDENCE: SKILL.md line 35: "1. **Layout** — `display`, `flex`, `grid`, `position`, `gap`, `z-index`"
  REASONING: gap appears in the Layout group list.

- [x] z-index is explicitly listed under Group 1 (Layout) — not under Effects
  EVIDENCE: SKILL.md line 35: "1. **Layout** — `display`, `flex`, `grid`, `position`, `gap`, `z-index`"
  REASONING: z-index appears in the Layout group list.

- [x] border-radius is explicitly listed under Group 4 (Structure) — not under Dimensions
  EVIDENCE: SKILL.md line 38: "4. **Structure** — `margin`, `padding`, `border`, `border-radius`"
  REASONING: border-radius appears in Structure group list.

- [x] The rule states each group is separated by exactly one blank line
  EVIDENCE: SKILL.md line 33: "each group separated by a blank line"
  REASONING: "a blank line" implies exactly one blank line between groups.

### TC-004: Repeated Styles Across Three Components

- [x] Phase 3 clearly states it only activates when styles repeat across MORE than two components
  EVIDENCE: SKILL.md line 47: "> **Only invoke if** the same styles are repeated across more than two components."
  REASONING: "more than two" excludes two — threshold is explicit.

- [x] The skill instructs proposing, defining, AND registering a new .c-* class — all three steps present
  EVIDENCE: SKILL.md line 49: "Propose a new `.c-*` engine class for `src/styles.scss` — name it, define it, register it."
  REASONING: "name it, define it, register it" covers all three required steps.

### TC-005: Trigger Boundary — Should NOT Activate

- [x] The trigger rule is scoped to .scss/.css files — a .component.ts file would NOT activate this skill
  EVIDENCE: SKILL.md line 9: "**Trigger:** Before creating or editing any `.scss` or `.css` file in `src/`."
  REASONING: .ts files are not .scss or .css — skill does not activate.

- [x] No CSS architecture guidance would be given for a TypeScript-only change
  EVIDENCE: SKILL.md line 9 (same as above)
  REASONING: Trigger scoping to .scss/.css means TypeScript-only changes bypass this skill entirely.

### TC-006: Logical Properties — Physical Direction Rejection

- [ ] The skill is specific enough that a developer would use padding-inline (not padding-left/right) for horizontal spacing
  EVIDENCE: No rule in SKILL.md addresses padding-inline specifically.
  NOTE: SKILL.md line 16 says "Logical properties only: `margin-inline`, `padding-block` — no physical directional values" but omits padding-inline from the examples. A developer needing horizontal padding would not find explicit guidance.

- [x] Border property would be placed in Group 4 (Structure) based on the Five-Group listing
  EVIDENCE: SKILL.md line 38: "4. **Structure** — `margin`, `padding`, `border`, `border-radius`"
  REASONING: border is explicitly listed in Group 4.

### TC-007: Phase 1 — Compose Existing Engine Class Before Writing New Styles

- [x] Phase 1 ordering is clear: scan for existing .c-* classes FIRST, then compose if found — writing new styles is the fallback
  EVIDENCE: SKILL.md line 23: "**Engine Search:** Scan `src/styles.scss` for existing `.c-*` engine classes that can be composed before writing new styles."
  REASONING: "before writing new styles" establishes scanning as prior step, writing as fallback.

- [x] The skill explicitly prohibits defining new .c-* classes inside component .scss files
  EVIDENCE: SKILL.md line 12: "`.c-*` engine classes belong **only** in `src/styles.scss` — never inside a component `.scss`"
  REASONING: "never inside a component .scss" is an explicit prohibition.

### TC-008: Inline Styles — Static Value Must Go in SCSS

- [x] The skill makes a clear distinction: inline styles only acceptable for dynamic/runtime values, NOT static values
  EVIDENCE: SKILL.md line 15: "No inline styles unless the value is dynamic/runtime"
  REASONING: "unless the value is dynamic/runtime" creates clear boundary.

- [x] Five-Group placement would put height in Group 2 (Dimensions) and background-color in Group 3 (Content)
  EVIDENCE: SKILL.md line 36: "2. **Dimensions** — `width`, `height`, `aspect-ratio`" / line 37: "3. **Content** — typography, colors, `content`"
  REASONING: height listed in Dimensions; background-color is a color property, covered by "colors" in Content.

### TC-009: Responsive Breakpoints — Must Follow Token Definitions

- [x] The skill requires using project-defined breakpoint tokens — not hardcoded pixel values
  EVIDENCE: SKILL.md line 17: "Responsive breakpoints must follow project token definitions"
  REASONING: "must follow project token definitions" prohibits hardcoded values.

- [x] Responsive breakpoint blocks are placed after the base selector, not before
  EVIDENCE: SKILL.md line 41: "Place responsive breakpoint blocks after the base selector — never before the base styles."
  REASONING: Explicit ordering rule with negation ("never before").

### TC-010: Group Placement — z-index (Layout) vs opacity (Effects)

- [x] z-index is under Group 1 (Layout) — NOT under Effects
  EVIDENCE: SKILL.md line 35: "1. **Layout** — `display`, `flex`, `grid`, `position`, `gap`, `z-index`"
  REASONING: z-index listed in Layout group.

- [x] opacity is under Group 5 (Effects) — NOT under Layout
  EVIDENCE: SKILL.md line 39: "5. **Effects** — `transition`, `animation`, `shadow`, `opacity`"
  REASONING: opacity listed in Effects group.

- [x] transition is under Group 5 (Effects) alongside opacity and animation
  EVIDENCE: SKILL.md line 39: "5. **Effects** — `transition`, `animation`, `shadow`, `opacity`"
  REASONING: transition, animation, opacity all in the same Effects group.

### TC-011: Five-Group Blank-Line Separation Enforcement

- [x] The blank-line-between-groups rule is stated as mandatory — not a suggestion or style preference
  EVIDENCE: SKILL.md line 33: "Apply the **Five-Group Vertical Rhythm** in every selector — in this order, each group separated by a blank line"
  REASONING: "in every selector" makes this universally mandatory; "separated by a blank line" is the rule.

- [x] The skill is specific enough that a developer would NOT write all five groups as a flat, uninterrupted block
  EVIDENCE: SKILL.md line 33 (same as above)
  REASONING: "each group separated by a blank line" requires blank lines — flat block would violate this.
