# Tech Debt Audit — 2026-04-14

## Summary
- Unused imports removed: 0
- TODOs logged: 0 (critical: 0 / nice-to-have: 0)
- Components flagged for refactor: 0 (scoped files only)
- Style violations: 0 (blocking) / 1 (structural observation, non-blocking)
- Security flags: 0

## Scope
Working-tree mode — 3 files changed this session:
- `server/routes/generic.js`
- `src/app/core/services/recipe-data.service.ts`
- `src/app/core/services/dish-data.service.ts`

MemPalace consulted — no directly relevant findings for session files. Known hotspot noted: `recipe-builder.page.ts` size regression (Plan 260, not in scope this session).

---

## Detailed Findings

### Dead Code
None. All imports are used. No commented-out blocks. No unused variables in scoped files.

### TODO / FIXME Inventory
None found in scoped files.

### Style Violations

| File | Line | Observation | Status |
|------|------|-------------|--------|
| `src/app/core/services/recipe-data.service.ts` | 17 | `signal<Recipe[]>([])` uses trailing semicolons inconsistently (some lines have `;`, some don't — mixed within same file) | Non-blocking, style inconsistency carried over from pre-existing code, not introduced this session |
| `src/app/core/services/dish-data.service.ts` | similar | Same mixed semicolon pattern | Non-blocking |

No `@Input`/`@Output` decorators (all signals). No `BehaviorSubject`. No `any` types. No file exceeds 300 lines (recipe-data: 204 lines, dish-data: 211 lines, generic.js: 302 lines — JS file, rule applies to TS only).

### Refactor Candidates
None in scope. Both Angular services are appropriately sized and well-structured.

**Structural observation (non-blocking):** `recipe-data.service.ts` and `dish-data.service.ts` share near-identical patterns for trash operations (`deleteX`, `getTrashX`, `restoreX`, `disposeX`, `restoreAllX`, `disposeAllX`). A generic `TrashService<T>` abstraction could reduce duplication — this is a known architectural pattern gap, pre-existing, not introduced this session.

### Security Flags
None. `generic.js` correctly:
- Strips `userId`, `_masterId`, `_userModified` from client-supplied bodies before write
- Uses `BLOCKED_ENTITY_TYPES` to guard sensitive collections
- Scopes all reads/writes to `req.user.userId`
- The `_masterId` fallback lookup (session change) is correctly scoped to `userId !== '__master__'` — no privilege escalation path

---

## Trend (last 7 audits)

| Date | Violations | TODOs | Refactor Candidates | Security Flags |
|------|-----------|-------|--------------------|-|
| 2026-03-26 | — | — | — | — |
| 2026-03-27 | — | — | — | — |
| 2026-04-07 | — | — | — | — |
| 2026-04-09 | — | — | — | — |
| 2026-04-10 | — | — | — | — |
| 2026-04-14 (today) | 0 blocking | 0 | 0 | 0 |

→ Trend: stable. Session-scoped files are clean.
