- @custom-select.component.html (1-48)  Inside that. Add new purchase unit inside. The recipe builder. The select. Measurement unit. Needs to be. A searchable. Drop down container. Meaning. The user can type. And it will show him. The available options in the dropdown.
 On top of it When the drop down. Opens. When there's still no text. In the search container. It should show the default options. ( gram, gk, milileter, liter, unit) Allowing the. To easily select one of them, because they will be. The most used ones. Once the user starts typing in. It will work as a. Search container. Showing. The closest options first. And underneath. The matched option. The default options. 2. So even if the user search for a specific option. You will get on top. The default options. Making it easier for him. Of course if he search for an option. Out of the default options. He will show first. And then the rest of the default options. But we won't show. Duplicate. Options.

- 