# Quick Chat

Explicit fast-path startup for Cursor. **QUICK_MODE_ENABLED: skip handoff + GitHub sync**

This command enables fast startup by skipping session-handoff check and GitHub sync, while preserving mandatory gate reads (agent.md, copilot-instructions.md).

Use when starting a quick follow-up chat where recent context is already loaded.
