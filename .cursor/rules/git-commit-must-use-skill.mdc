---
description: Git operations must use the git-agent (visual plan + approval)
alwaysApply: true
---

When the user asks to commit, push, create a PR, merge, or perform any git operation,
read `.claude/agents/git-agent.md` and follow it. No git writes until the user approves
the visual plan.
