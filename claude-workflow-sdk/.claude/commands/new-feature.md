# /new-feature — Scaffold a New Feature

> **SDK stub** — this command requires project-specific configuration.
> Fill in the sections marked `[PROJECT_SPECIFIC]` after running `/init-repo`.

## Purpose
Scaffold a new feature following project conventions.

## Usage
`/new-feature <description>`

## Execution
[PROJECT_SPECIFIC]

Suggested flow:
1. Run `product-manager` agent to author PRD
2. Run `software-architect` agent to author HLD
3. Run `/plan-implementation` to verify against codebase
4. Run `/execute-it` to implement
