 produce-form 
1. form-group scaling-box ng-untouched ng-pristine ng-valid - the trash icon dont show if hte container is to wiedth its pused to much to the sides! 

2. form-group - מחיר קנייה - remove the defult up down buttons witged , 


4. scaling-row -
a.  we need a smarter grid on this container
the app-custom-select needs to be width of its content ( depanding on wats in it and grouing acording to the text add ) 
same as the rest of the inputes 
b. the grid needs to be reactive and ork good across all brake-pointes 
c. מחיר רכישה מיוחד - cange it to מחיר מיוחד


5. have form-group labels text be centerd 

6. c-btn-primary - have the width of submit and back buttons be the same and when the first brake point happens  
 



mobile


- supliers - the row grid is broken! it needs to be as the venues 


supliiers & venues 
- the back button is not as it needs to be it needs to be the same as  
<button _ngcontent-ng-c3008099600="" type="button" data-testid="btn-back-to-dashboard" class="header-btn header-btn--back"><lucide-icon _ngcontent-ng-c3008099600="" name="arrow-right" size="16" ng-reflect-name="arrow-right" ng-reflect-size="16" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-arrow-right"><path d="M5 12h14" key="1ays0h"></path><path d="m12 5 7 7-7 7" key="xquz4c"></path></svg></lucide-icon> חזרה ללוח הבקרה </button>

- supliers - the row grid is broken! it needs to be as the venues 



cook-view 
- There is this behavior allowing the user to click.So we could mark done on the different.Ingredients in the.Ingredient index.But it's not working.Exactly.Sometimes it marks, sometimes it doesn't mark.I want to have this behavior.That we're going to.Is that having it click? Sometimes yes, sometime no. We're going to have a designated button.That will mark it done.So we'll have some sort of a button at the middle of that.Row or wherever you think it's fitting according that whenever the user marks presses this button, it marks it.Unless he pressed that button, it should not mark. 

- The different rows need to look a little bit like.Cards. It's hard to.Understand the difference between each different row.Both in the grid and the ingredient index and both in the workflow.You need to have a little bit more.Box shadow and as a general.It will need to look a little bit more.Like it's one of its own, it will help the user understand it's a different.Bing.






 genaral
app-custom-select
activity-section

all scroll-containers - the covert up/down should show only on hover and only if there is more in there diraction! in case the user has scrolld all the way down then iven on hover it wont show. it shows only if there is more to scroll in that diraction 

and make sure the covert are at the edges and not on the content
each container needs to have a small margin botom and top to fit on them the covert in absolute position

custom select - There are many custom selects buttons that have this behavior that I want to discard out of them. It's like it's a container that inside the container there's the custom select. So that custom select has a border. So what we see is a container that inside we have another container. This is something I want to discard. Our custom select doesn't need to have an inner border.It's one component. An example will be inside the cook view when you set the different.Measurement unit. You can see that that custom select has an inner border. This is the problem we need to discard from everywhere in the project.


